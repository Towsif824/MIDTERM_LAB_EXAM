<!DOCTYPE html>
  <html>
  <head>
      <title>Update page</title>
      <style>
      table.center {
            margin-left: auto;
            margin-right: auto;
          }
      </style>
  </head>
  <body>
    <form method="post">
      <fieldset>
        <legend>Update Products</legend>
        <table class="center">
          <tr>
            <td>ID </td>
            <td>:<%= user.id %></td>
          </tr>
          <tr>
            <td>Product Name </td>
            <td><input type="text" value="<%= user.productName %>" required="required" name="productName"></td>
          </tr>
          <tr>
            <td>Quantity</td>
            <td><input type="number" value="<%= user.quantity %>" required="required" name="quantity"></td>
          </tr>
          <tr>
            <td>Price</td>
            <td><input type="number" value="<%= user.price %>" required="required" name="price" ></td>
          </tr>
          <tr>
            <td></td>
            <td><button type="submit" name="choice" value="Update">Update</button></td>
          </tr>
        </table>
    </fieldset>
  </form>
  <table class="center">
      <tr>
        <td><a href="/employee/AllProducts">Back</a>|
      	<a href="/logout">Logout</a></td>
      </tr>
  </table>
</body>
</html>